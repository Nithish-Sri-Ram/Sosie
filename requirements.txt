# Sosie root - installs the Mac-testable layers (STT + LLM + TTS) into one venv
# if you want to run the whole voice loop locally without per-folder envs.
# vid_gen (Ditto) is CUDA-only and intentionally excluded - it needs its own
# venv on the GPU box, see vid_gen/SETUP.md.
-r stt/requirements.txt
-r llm/requirements.txt
-r tts/requirements.txt
