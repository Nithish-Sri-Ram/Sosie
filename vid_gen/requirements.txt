# Sosie vid_gen (Ditto). CUDA / NVIDIA only - does NOT run on Apple MPS.
# These are just the Flask server's own deps. Ditto itself (torch, tensorrt,
# cuda-python, its checkpoints) is installed separately per its own
# environment.yaml inside the cloned repo - see SETUP.md.
flask
flask-cors
python-dotenv
imageio-ffmpeg
librosa
numpy
